#include <stdio.h>

int string_length(char s[]) {
    int length = 0;

    while (s[length] != '\0') {
        length++;
    }

    return length;
}

int main() {
    char s[] = "Hello, World!";

    printf("Length of string: %d\n", string_length(s));

    return 0;
}
